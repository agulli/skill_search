.venv/
data/
*.db
*.db-wal
*.db-shm
__pycache__/
*.egg-info/
.pytest_cache/
logs/

# Rust build output
rust/target/
logs/maintain.pid

# Release artefacts: derived from the corpus and regenerable via release.py.
# Everything here is a .db today, so *.db already covers it — named explicitly
# so a non-.db artefact cannot be committed by accident.
dist/
