[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "skill-engine-rs"
version = "0.1.0"
requires-python = ">=3.10"
description = "Pattern matching for skill-engine: the one part where Rust is the better tool"

[tool.maturin]
features = ["pyo3/extension-module"]
